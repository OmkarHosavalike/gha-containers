name: Deploy app using containers
on:
  push:
    branches:
      - main
      - dev
env:
  cache_key: node-deps
  MONGODB_DB_NAME: gha-demo
jobs:
  test: 
    environment: testing
    runs-on: ubuntu-latest
    container:
      image: node:16
    env:
      MONGODB_CONNECTION_PROTOCOL: mongodb #mongodb+srv
      MONGODB_CLUSTER_ADDRESS: mongodb #cluster1.ejiabj5.mongodb.net
      #MONGODB_USERNAME: ${{ secrets.MONGODB_USERNAME }}
      #MONGODB_PASSWORD: ${{ secrets.MONGODB_PASSWORD }}
      MONGODB_USERNAME: root
      MONGODB_PASSWORD: example
      PORT: 8080
    services:
      mongodb:
        image: mongo
        env:
          MONGO_INITDB_ROOT_USERNAME: root
          MONGO_INITDB_ROOT_PASSWORD: example
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
      - name: Cache the deps if does not exists
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: $cache_key-${{ hashFiles('**/package-lock.json') }}
      - name: Install deps
        run: npm ci
      - name: Run the server
        run: npm start & npx wait-on http://127.0.0.1:$PORT
      - name: Test the app
        run: npm test
      - name: Output secret valuu
        run: |
          echo "Username: $MONGODB_USERNAME"

  deploy: 
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output the environment variables
        env: 
          PORT: 3000
        run: |
          echo "Username: $MONGODB_USERNAME"
          echo "Database name: $MONGODB_DB_NAME"
          echo "PORT: $PORT"
